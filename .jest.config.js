// jest.config.js
// Sync object
/** @type {import('@jest/types').Config.InitialOptions} */
const config = {
  bail: 2,
  clearMocks: true,
  collectCoverage: true,
  verbose: true,
};

module.exports = config;
